version: '3.7'

services:
  nemde:
    build:
      context: .
    volumes:
      - ./scripts/validation:/app/scripts/validation
    entrypoint: /app/scripts/entrypoint.sh

networks:
  default:
    external:
      name: mysql_default
